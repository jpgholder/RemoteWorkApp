@model RemoteWork.Models.Team

@{
    ViewData["Title"] = "Создать команду";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container mt-5">
    <div class="row d-flex justify-content-center">
        <div class="col-md-8 col-lg-6 col-xl-5 col-xxl-5">
            <div class="card mb-5">
                <div class="card-body p-sm-5">
                    <h2 class="text-center mb-4">Создать команду</h2>
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-floating mb-3">
                            <input asp-for="Name" class="form-control" aria-required="true" placeholder="name" required="required"/>
                            <label asp-for="Name" class="form-label">Название команды</label>
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                        <div>
                            <button class="btn btn-primary d-block w-100" type="submit">Создать</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>